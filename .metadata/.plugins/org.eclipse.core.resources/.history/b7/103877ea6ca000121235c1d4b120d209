library IEEE;
use IEEE.numeric_bit.all;

ENTITY registre_16 IS
	generic(N : integer :=16);
	PORT(input 	  	: IN unsigned (N downto 0);
		 reset 		: IN bit;
		 horloge	: in bit;
		 output    	: OUT unsigned (N downto 0));
		 
END registre_16;